   #include "structs.h"

  extern GameObject monster[];
  extern GameObject player;
  extern Gestion jeu;
  extern Ground wall;
  extern Camera camera;

  /* Prototypes des fonctions utilisées */
  extern SDL_Surface *loadImage(char *name);
  extern void mapCollision(GameObject *entity, Ground wall);
  extern int collide(GameObject *player, GameObject *monster);
  extern void inputEnnemi(GameObject *player, GameObject *monster,int d1);
  extern void MoveEnnemi (GameObject *monster);
  extern void gameOver(void);
  extern void initializePlayer(void);
  extern void drawIm(SDL_Surface *image, int x, int y);
